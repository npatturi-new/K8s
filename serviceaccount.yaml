apiVersion: v1
kind: ServiceAccount
metadata:
name: order-service-sa
namespace: production
annotations:
# IRSA: link to IAM role on EKS
eks.amazonaws.com/role-arn: arn:aws:iam::123456:role/order-service-role